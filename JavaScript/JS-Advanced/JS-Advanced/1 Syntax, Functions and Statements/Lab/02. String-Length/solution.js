function stringLength(str1, str2, str3) {
    let calcLength = str1.length + str2.length + str3.length;
    console.log(calcLength);
    let average = Math.floor(calcLength / 3);
    console.log(average);
}
